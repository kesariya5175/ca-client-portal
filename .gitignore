node_modules/
dist/
.env
.env.local
.DS_Store
*.log

# Credentials and sensitive files — never commit these
*credentials*
*credntials*
*Credentials*
*.docx

# Vite writes these transient config timestamps on every dev run
vite.config.js.timestamp-*
